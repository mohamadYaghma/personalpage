/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['placekitten.com', 'picsum.photos'], // اضافه کردن دامنه‌های مجاز
    },
  }
  
  module.exports = nextConfig
  